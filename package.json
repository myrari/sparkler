{
	"name": "sparkler",
	"version": "0.2.0",
	"description": "Server for bridging game events to external stimuli",
	"license": "MIT",
	"author": "@myrari",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"build": "npx tsc -p tsconfig.json && cp -ar src/dist/* dist/",
		"start": "node --env-file=.env dist/index.js",
		"dev": "npx tsx --env-file=.env --watch  src/index.ts"
	},
	"dependencies": {
		"body-parser": "^2.2.2",
		"express": "^5.1.0",
		"socket.io-client": "2.1.0",
		"typescript": "^6.0.2"
	},
	"devDependencies": {
		"@tsconfig/node24": "^24.0.3",
		"@types/express": "^5.0.3",
		"@types/node": "^24.6.2",
		"@types/socket.io-client": "^1.4.36"
	}
}
